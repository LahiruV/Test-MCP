/**
 * Placeholder login screen.
 * The real form, validation and API wiring land in MCPJ-2, MCPJ-3 and MCPJ-4.
 */
export function LoginPage() {
  return (
    <main className="flex min-h-dvh items-center justify-center p-4">
      <section className="w-full max-w-[400px] rounded-card bg-white p-8 shadow-card">
        <h1 className="text-2xl font-semibold text-ink-900">Sign in</h1>
        <p className="mt-2 text-sm text-ink-500">
          Scaffolding is in place. The login form arrives in MCPJ-2.
        </p>
      </section>
    </main>
  );
}
